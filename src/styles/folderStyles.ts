// Npm imports
import styled from "styled-components"
import { motion } from "framer-motion"

export const FolderBack = styled.img`
    width: 9em;
    position: absolute;
`
export const FolderFront = styled(motion.img)`
    width: 9em;
    position: absolute;
`